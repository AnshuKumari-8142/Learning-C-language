#include <stdio.h>
int main(){
    int arr[] = {1,2,3};
    for(int i = 0; i < 3; i++){
       int sum = sum + arr[i];
       printf("Sum of all elements = %d", sum);
    }
    return 0;
}

